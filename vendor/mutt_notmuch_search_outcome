#!/bin/bash

rm -f $MUTT_CACHE/mutt_search_succeeded

if [[ -f $MUTT_CACHE/mutt_search_abort ]]
then
    rm -f $MUTT_CACHE/mutt_search_abort
    exit 0
fi

if [[ -n "$(find $MUTT_CACHE/mutt_limit_pattern -size +0 2>/dev/null)" ]]
then
    touch $MUTT_CACHE/mutt_search_succeeded
fi
